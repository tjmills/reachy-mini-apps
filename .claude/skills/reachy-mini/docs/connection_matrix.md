# Connection matrix
- Wireless remote: daemon runs on robot; SDK connects over network.
- Wireless local (SSH): run scripts on robot; localhost connection.
- Lite USB: start daemon (desktop app or reachy-mini-daemon); verify http://localhost:8000.
- Simulation: run daemon with --sim; verify http://localhost:8000.
