# Debug checklist
If nothing moves:
1) daemon running? (Lite/Sim: http://localhost:8000)
2) correct connection mode? (Docker breaks discovery; use explicit IP)
3) motors enabled?
If camera is dark (Lite): check exposure settings / v4l2.
