# Daemon + REST

Most SDK calls require the daemon running.
Typical REST base (verify in upstream docs): http://<host>:8000/api/

Suggested debug:
- curl /api/state/full (if available)

Always confirm endpoints in upstream source/docs before relying on them.
